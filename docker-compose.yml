services:
  pos-system:
    build: .
    container_name: pos-system
    restart: always
    expose:
      - "5000"
    volumes:
      - pos-data:/app/database
    environment:
      - TZ=Asia/Kuwait
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - nginx-proxy

volumes:
  pos-data:

networks:
  nginx-proxy:
    external: true
